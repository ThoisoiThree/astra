# Pinned so local builds, CI and release artifacts use the same compiler and lints.
# Cargo.toml's `rust-version` records the minimum supported version.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
